package days09;

import java.io.IOException;
import java.util.Scanner;

public class Test02 {

	public static void main(String[] args) throws IOException {

		/*String name;
		int kor, eng, mat, tot, rank;
		double avg;

		String [] names = new String [30];
		int [] kors = new int [30];
		int [] engs = new int [30];
		int [] mats = new int [30];
		int [] tots = new int [30];
		int [] ranks = new int [30];
		double [] avgs = new double [30];

		int count = 0;
		char con = 'y';

		Scanner scanner = new Scanner(System.in);
		System.out.printf("[%d] 학생 이,국,영,수 입력 ? ", count+1);

		do {
			System.out.printf("%d 총 학생수 ? ", count);

			name = scanner.next();
			kor = scanner.nextInt();
			eng = scanner.nextInt();
			mat = scanner.nextInt();
			rank = scanner.nextInt();
			tot = kor + eng + mat;
			avg = (double) tot/3;

			name = names[count];
			kor = kors[count];
			eng = engs[count];
			mat = mats[count];
			rank = ranks[count];
			tot = tots[count];
			avg = avgs[count];

			System.out.printf("학생 입력 계속 %c ? ", con);
			System.in.read();
			System.in.skip(System.in.available());

		} while (con == 'y' || con == 'Y');

		for (int i = 0; i < count; i++) {

			System.out.printf("%d\t%s\t%d\t%d\t%d\t%d\t%d\t%.2f", i+1,names[i],kors[i],engs[i],mats[i],tots[i],ranks[i],avgs[i]);
		}*/

		/*int [][] m = {
					{1,2,3},
					{4,5,6}		
		};*/
		
		int [] m = {1,2,3,4,5,6};
		
		for (int i = 0; i < m.length; i++) {
			System.out.println(m[i]);
		}
		System.out.println();






	}

}
